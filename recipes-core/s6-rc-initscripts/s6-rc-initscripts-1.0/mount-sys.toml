# SPDX-License-Identifier: MIT

[mount-sys]
dependencies = [
    "mount-proc",
]
down = "s6-umount /sys"
type = "oneshot"
up = """
if -t { eltest -d /sys }
if -t { redirfd -r 0 /proc/filesystems s6-grep -qF sysfs }
if -n -t { eltest -r /sys/class }
s6-mount -wt sysfs sys /sys
"""
