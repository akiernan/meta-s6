# SPDX-License-Identifier: MIT

[mount-dev]
dependencies = [
    "mount-proc",
]
type = "oneshot"
up = """
if -n -t { eltest -e /dev/zero }
if -t { eltest -d /dev }
if -t { redirfd -r 0 /proc/filesystems s6-grep -qF devtmpfs }
s6-mount -wt devtmpfs dev /dev
"""
