# SPDX-License-Identifier: MIT

[mount-sys-debugfs]
dependencies = [
    "mount-sys",
]
down = "s6-umount /sys/kernel/debug"
type = "oneshot"
up = """
if -t { eltest -d /sys/kernel/debug }
if -t { redirfd -r 0 /proc/filesystems s6-grep -qF debugfs }
s6-mount -wt debugfs debug /sys/kernel/debug
"""
